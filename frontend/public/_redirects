# Redirects for Render static site hosting
# Handle client-side routing - all routes should serve index.html

# Serve static files directly
/assets/* /assets/:splat 200
/favicon.ico /favicon.ico 200
/vite.svg /vite.svg 200
/test-route.html /test-route.html 200

# Handle all other routes with React Router
/* /index.html 200
